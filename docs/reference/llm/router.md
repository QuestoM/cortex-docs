# LLM Router

Multi-model LLM router that directs requests to the best provider and model based on task requirements and weights. Acts as the system's thalamus -- all request input passes through here.

::: corteX.core.llm.router
    options:
      show_source: true
      members_order: source
