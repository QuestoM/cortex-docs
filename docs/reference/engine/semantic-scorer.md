# Semantic Scorer

Vector embedding-based importance scoring using TF-IDF (pure numpy, no scikit-learn). Scores text relevance to goal, novelty versus seen content, and combined importance. Pluggable backend supports sentence-transformers as optional upgrade.

::: corteX.engine.semantic_scorer
    options:
      show_source: true
      members_order: source
