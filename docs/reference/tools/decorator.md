# Tool Decorator

Decorator-based tool registration framework that makes it trivial for SaaS developers to add custom tools to their agents.

::: corteX.tools.decorator
    options:
      show_source: true
      members_order: source
